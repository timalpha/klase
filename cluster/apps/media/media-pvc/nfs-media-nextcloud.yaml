---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-media-nextcloud-pv
  namespace: media
spec:
  storageClassName: nfs-media-nextcloud
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: "truenas.${SECRET_DOMAIN}"
    path: "/mnt/zp0/nextcloud"
  mountOptions:
    - nfsvers=3
    - tcp
    - intr
    - hard
    - noatime
    - nodiratime
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-media-nextcloud-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-media-nextcloud
  resources:
    requests:
      storage: 1Mi

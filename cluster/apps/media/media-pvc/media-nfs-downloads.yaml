---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: media-nfs-downloads-pv
  namespace: media
spec:
  storageClassName: media-nfs-downloads
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: "${NAS_ADDR}"
    path: "/mnt/zp0/media/downloads"
  mountOptions:
    - nfsvers=3
    - tcp
    - intr
    - hard
    - noatime
    - nodiratime
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-nfs-downloads-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: media-nfs-downloads
  resources:
    requests:
      storage: 1Mi
